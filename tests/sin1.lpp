PROCEDIMIENTO main(a,b,c) {
    ENTERO i -> "asdfasdfa";
    ENTERO K;
    SI((l < 10) & (a < 20)) ENTONCES {
        POR(i) EN (0,10,2) {
            j -> NULO;
        }
        i -> i / 1;
        MIENTRAS(j < 10) {
            z -> z;
        }
        ENTERO l -> 0;
    }
}