PROCEDIMIENTO main() {
    ENTERO i -> 0;
    SI((l < 10) & (a < 20)) ENTONCES {
        POR(i) EN (0,10,2) {
            j -> i;
        }
        i -> i / 1;
        MIENTRAS(j < 10) {
            z -> z;
        }
        ENTERO l -> 0;
    }
}